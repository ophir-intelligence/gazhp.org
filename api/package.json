{
  "name": "gazhp-payments-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute gazhp-payments --remote --file=schema.sql",
    "db:init:local": "wrangler d1 execute gazhp-payments --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
